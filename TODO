TODO
----

Priority HIGH:

Priority medium:
    Code cleaning
    deb upgrade package should restart the running daemon
    deb should conflict with existing syslog server

Priority low:


POSSIBLE BUG
------------

* pika.BlockingConnection (should use async function)
* thread issues with the handling of /proc/kmsg
* write to fifo

BUG
---

* logelf logfile bufferize data
* don't reconnect on AMQP restart
