TODO
----

Priority HIGH:
    Fix handling with /dev/log (kill by initscript)

Priority medium:
    Initscript cleaning
    Code cleaning

Priority low:
    Add true daemon facilities (daemon module) 


POSSIBLE BUG
------------

* pika.BlockingConnection (should use async function)
* write to fifo
* thread issues with the handling of /proc/kmsg

BUG
---

* /dev/log isn't always remove
* don't reconnect on AMQP restart
